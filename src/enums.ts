export enum SupportedLang {
    gb = "gb",
    de = "de",
    it = "it",
}

export enum PageId {
    profile = "profile",
    rulesets = "rulesets",
    friends = "friends",
    stats = "stats",
    home = "home",
    history = "history",
}